<div class="row">
    <p class="d-flex justify-content-around">

        {% for category in categories %}
            <a href="{{ path('show_articles_from_cat)}}" class="text-decoration-none item-hover">{{ category.name | capitalize }}</a>
        {% endfor %}
    </p>
</div>